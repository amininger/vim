# This is a file
# I know, right

sp {hello*world*one
   (state <s> ^superstate nil
              ^operator.name init-agent)
-->
   (<s> ^name hello-world)
}

sp {hello*world*two
   (state <s> ^superstate nil
              ^operator.name init-agent)
-->
   (<s> ^name hello-world)
}

sp {hello*world*three
   (state <s> ^superstate nil
              ^operator.name init-agent)
   {(<s> ^name <> hello-world)}
-->
   (<s> ^name hello-world)
}

sp {hello*world*four
   (state <s> ^superstate nil
              ^operator.name init-agent)
   {(<s> ^name <> hello-world)}
-->
   (<s> ^name hello-world)
}

sp {hello*world*five
   (state <s> ^superstate nil
              ^operator.name init-agent)
-->
   (<s> ^name hello-world)
}
